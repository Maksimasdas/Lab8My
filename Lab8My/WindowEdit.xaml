<Window x:Class="Lab8My.WindowEdit"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Lab8My"
        mc:Ignorable="d"
               Title="WindowEdit" Height="450" Width="400" FontSize="20">
    <Window.Resources>
        <Style TargetType="{x:Type TextBox}">
            <Setter Property="Margin" Value="5,10"/>
        </Style>
    </Window.Resources>
    <Grid x:Name="grid">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>
        <Label Content="Редактирование данных" HorizontalAlignment="Center"
               Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2"/>
        <Label Content="Марка" Grid.Row="1"/>
        <TextBox x:Name="textBoxName" Text="{Binding Path=Name}"
                 Grid.Row="1" Grid.Column="1"/>
        <Label Content="Модель" Grid.Row="2"/>
        <TextBox x:Name="textBoxModel" Text="{Binding Path=Model}"
                 Grid.Row="2" Grid.Column="1"/>
        <Label Content="VIN номер" Grid.Row="3"/>
        <TextBox x:Name="textBoxVin" Text="{Binding Path=Vin}"
                 Grid.Row="3" Grid.Column="1"/>
        <Label Content="Гос. Номер" Grid.Row="4"/>
        <TextBox x:Name="textBoxNumber" Text="{Binding Path=Number}"
                 Grid.Row="4" Grid.Column="1"/>
        <Button Content="ОК" Grid.Row="5" Grid.Column="0" Margin="25 15" Click="ButtonOK_Click"/>
        <Button Content="Cancel" Grid.Row="5" Grid.Column="1" Margin="25 15" Click="ButtonCancel_Click"/>

    </Grid>
</Window>